# PLAN V6 — Decisions

Q1 (does Laravel have it?) and Q2 (does another Go screen do it?) for every
screen touched. See `00_MASTER.md` §A.2.

```markdown
## <screen / route>
**Q1 Laravel:** <grep command + result> → has it / does not have it
**Q2 Duplicate:** <grep command + result> → duplicates X / unique
**Decision:** delete | merge into X | connect
**Reason:**
```
