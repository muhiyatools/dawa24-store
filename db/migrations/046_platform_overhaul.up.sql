-- 046_platform_overhaul (up)
BEGIN;

-- 1. Enhance org.branches with deep logistics and location fields
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS code TEXT NOT NULL DEFAULT '';
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS google_maps_url TEXT NOT NULL DEFAULT '';
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS manager_name TEXT NOT NULL DEFAULT '';
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS warehouse_type TEXT NOT NULL DEFAULT 'warehouse';
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS has_cold_storage BOOLEAN NOT NULL DEFAULT false;
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS capacity_sqm NUMERIC(10,2) NOT NULL DEFAULT 0;
ALTER TABLE org.branches ADD COLUMN IF NOT EXISTS operating_hours TEXT NOT NULL DEFAULT '';

-- 2. Enhance catalog.product_variants with vendor selling fields
ALTER TABLE catalog.product_variants ADD COLUMN IF NOT EXISTS batch_number TEXT NOT NULL DEFAULT '';
ALTER TABLE catalog.product_variants ADD COLUMN IF NOT EXISTS expiry_date DATE;
ALTER TABLE catalog.product_variants ADD COLUMN IF NOT EXISTS min_order_qty INT NOT NULL DEFAULT 1;
ALTER TABLE catalog.product_variants ADD COLUMN IF NOT EXISTS branch_id BIGINT REFERENCES org.branches(id) ON DELETE SET NULL;

-- 3. Enhance hr.job_applications with applicant user account link
ALTER TABLE hr.job_applications ADD COLUMN IF NOT EXISTS applicant_user_id BIGINT REFERENCES identity.users(id) ON DELETE SET NULL;

-- 4. Add employer / custom roles for organizations
CREATE TABLE IF NOT EXISTS org.custom_roles (
    id              BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    organization_id BIGINT NOT NULL REFERENCES org.organizations(id) ON DELETE CASCADE,
    name            JSONB NOT NULL,
    description     TEXT NOT NULL DEFAULT '',
    permissions     TEXT[] NOT NULL DEFAULT '{}',
    created_at      TIMESTAMPTZ NOT NULL DEFAULT now(),
    updated_at      TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE INDEX IF NOT EXISTS idx_org_custom_roles_org ON org.custom_roles(organization_id);

COMMIT;
