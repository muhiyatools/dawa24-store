-- 099_drop_translations (down)
--
-- Recreates the table shape. The rows are NOT recoverable — they were dropped
-- deliberately because nothing read them.

BEGIN;

CREATE TABLE IF NOT EXISTS platform_admin.translations (
    id                BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    key               TEXT NOT NULL UNIQUE,
    translation_group TEXT NOT NULL DEFAULT 'general',
    text              JSONB NOT NULL,
    updated_at        TIMESTAMPTZ NOT NULL DEFAULT now()
);

COMMENT ON TABLE platform_admin.translations IS
    'تجاوزات نصوص الواجهة ثنائية اللغة — أُلغيت في 097 لعدم استخدامها';

COMMIT;
