package layouts

import (
	"github.com/muhiya/dawa24-store/internal/ui/components"
)

templ VendorShell(title string, activeNav string, lang string, dir string) {
	if site := GetSiteSettings(ctx); site != nil {
		@Base(title, lang, dir) {
			<div class="app-shell">
				<!-- Sidebar -->
				<aside class="sidebar">
					<div class="sidebar-header">
						<div class="sidebar-brand">
							<img class="sidebar-logo" src={ site.LogoURL } alt={ site.SiteName } />
							<div class="sidebar-brand-copy">
								<span class="sidebar-brand-name">{ site.SiteName }</span>
								<span class="sidebar-brand-subtitle">بوابة الموردين والمخازن</span>
							</div>
						</div>
					<button type="button" class="btn btn-icon sidebar-toggle" data-sidebar-toggle title="طي/توسيع القائمة">
						@components.IconSidebarToggle("icon-xs")
					</button>
				</div>

				<nav class="sidebar-nav">
					
					<!-- Group 1: المنشأة والفروع -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>المنشأة والفروع</span>
						</div>
						<a href="/vendor/dashboard" class={ "sidebar-link", templ.KV("active", activeNav == "dashboard") }>
							@components.IconHome("icon-sm")
							<span>لوحة التحكم</span>
						</a>
						<a href="/vendor/organization" class={ "sidebar-link", templ.KV("active", activeNav == "organization") }>
							@components.IconBuilding("icon-sm")
							<span>بيانات المنشأة</span>
						</a>
						<a href="/vendor/user-organization" class={ "sidebar-link", templ.KV("active", activeNav == "user_organization") }>
							@components.IconUsers("icon-sm")
							<span>المستخدم-المنظمة</span>
						</a>
						<a href="/vendor/branches" class={ "sidebar-link", templ.KV("active", activeNav == "branches") }>
							@components.IconLayers("icon-sm")
							<span>الفروع والمخازن</span>
						</a>
						<a href="/vendor/team" class={ "sidebar-link", templ.KV("active", activeNav == "employees" || activeNav == "team") }>
							@components.IconUsers("icon-sm")
							<span>فريق العمل والموظفون</span>
						</a>
						<a href="/vendor/roles" class={ "sidebar-link", templ.KV("active", activeNav == "roles") }>
							@components.IconShield("icon-sm")
							<span>الأدوار والصلاحيات</span>
						</a>
						<a href="/vendor/coverage" class={ "sidebar-link", templ.KV("active", activeNav == "coverage") }>
							@components.IconTruck("icon-sm")
							<span>التغطية الأسبوعية</span>
						</a>
						<a href="/vendor/pharmacy-coverage" class={ "sidebar-link", templ.KV("active", activeNav == "pharmacy_coverage") }>
							@components.IconUsers("icon-sm")
							<span>تغطية الصيدليات</span>
						</a>
						<a href="/vendor/subscription" class={ "sidebar-link", templ.KV("active", activeNav == "subscription") }>
							@components.IconSparkles("icon-sm")
							<span>الاشتراك والعضوية</span>
						</a>
					</div>

					<!-- Group 2: الكتالوج والمخزون -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>الكتالوج والمخزون</span>
						</div>
						<a href="/vendor/products" class={ "sidebar-link", templ.KV("active", activeNav == "products" || activeNav == "variants") }>
							@components.IconPackage("icon-sm")
							<span>أصناف المورد</span>
						</a>
						<a href="/vendor/ingest" class={ "sidebar-link", templ.KV("active", activeNav == "ingest") }>
							@components.IconUpload("icon-sm")
							<span>استيراد الكتالوج الذكي</span>
						</a>
						<a href="/vendor/saving-products" class={ "sidebar-link", templ.KV("active", activeNav == "saving_products") }>
							@components.IconTag("icon-sm")
							<span>منتجات التوفير</span>
						</a>
						<a href="/vendor/decision-memory" class={ "sidebar-link", templ.KV("active", activeNav == "decision_memory") }>
							@components.IconZap("icon-sm")
							<span>ذاكرة قرارات المطابقة</span>
						</a>
						<a href="/vendor/inventory" class={ "sidebar-link", templ.KV("active", activeNav == "inventory") }>
							@components.IconLayers("icon-sm")
							<span>إدارة المخزون</span>
						</a>
						<a href="/vendor/warehouses" class={ "sidebar-link", templ.KV("active", activeNav == "warehouses") }>
							@components.IconBuilding("icon-sm")
							<span>المخازن ومواقع التخزين</span>
						</a>
					</div>

					<!-- Group 3: العروض والتسويق -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>العروض والتسويق</span>
						</div>
						<a href="/vendor/offers" class={ "sidebar-link", templ.KV("active", activeNav == "offers") }>
							@components.IconTag("icon-sm")
							<span>العروض والخصومات</span>
						</a>
						<a href="/vendor/offers-packages" class={ "sidebar-link", templ.KV("active", activeNav == "offers_packages") }>
							@components.IconTag("icon-sm")
							<span>باقات العروض والرعايات</span>
						</a>
						<a href="/vendor/storefront" class={ "sidebar-link", templ.KV("active", activeNav == "storefront") }>
							@components.IconGlobe("icon-sm")
							<span>الأقسام المميزة وواجهة المتجر</span>
						</a>
					</div>

					<!-- Group 4: الطلبات والمالية -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>الطلبات والمالية</span>
						</div>
						<a href="/vendor/orders" class={ "sidebar-link", templ.KV("active", activeNav == "orders") }>
							@components.IconTruck("icon-sm")
							<span>أوامر التوريد والشحنات</span>
						</a>
						<a href="/invoices" class={ "sidebar-link", templ.KV("active", activeNav == "invoices") }>
							@components.IconFile("icon-sm")
							<span>الفواتير والمستحقات</span>
						</a>
						<a href="/vendor/payments" class={ "sidebar-link", templ.KV("active", activeNav == "payments") }>
							@components.IconWallet("icon-sm")
							<span>المدفوعات والحركات المالية</span>
						</a>
					</div>

					<!-- Group 5: الأدوات والتحليلات -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>الأدوات والتحليلات</span>
						</div>
						<a href="/compare/tool" class={ "sidebar-link", templ.KV("active", activeNav == "compare") }>
							@components.IconTrendingUp("icon-sm")
							<span>مقارنة الخصومات</span>
						</a>
						<a href="/market-discounts" class={ "sidebar-link", templ.KV("active", activeNav == "market-discounts") }>
							@components.IconChart("icon-sm")
							<span>خصومات السوق</span>
						</a>
						<a href="/vendor/jobs" class={ "sidebar-link", templ.KV("active", activeNav == "jobs") }>
							@components.IconBriefcase("icon-sm")
							<span>وظائف التوظيف</span>
						</a>
					</div>

					<!-- Group 6: المحتوى والسياسات -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>المحتوى والسياسات</span>
						</div>
						<a href="/vendor/documents" class={ "sidebar-link", templ.KV("active", activeNav == "documents") }>
							@components.IconFile("icon-sm")
							<span>المستندات والتراخيص</span>
						</a>
						<a href="/vendor/policies" class={ "sidebar-link", templ.KV("active", activeNav == "policies") }>
							@components.IconShield("icon-sm")
							<span>سياسات التوريد والضمان</span>
						</a>
						<a href="/vendor/activities" class={ "sidebar-link", templ.KV("active", activeNav == "activities") }>
							@components.IconClock("icon-sm")
							<span>سجل العمليات والنشاط</span>
						</a>
						<a href="/vendor/ai-logs" class={ "sidebar-link", templ.KV("active", activeNav == "ai_logs") }>
							@components.IconSparkles("icon-sm")
							<span>سجل استهلاك الذكاء الاصطناعي</span>
						</a>
					</div>

					<!-- Group 7: الحساب والأمان -->
					<div class="sidebar-group">
						<div class="sidebar-section-header">
							<span>الحساب والأمان</span>
						</div>
						<a href="/vendor/subscription" class={ "sidebar-link", templ.KV("active", activeNav == "subscription") }>
							@components.IconSparkles("icon-sm")
							<span>باقة الاشتراك والترقية</span>
						</a>
						<a href="/vendor/sessions" class={ "sidebar-link", templ.KV("active", activeNav == "sessions") }>
							@components.IconShield("icon-sm")
							<span>الأجهزة والجلسات النشطة</span>
						</a>
						<a href="/settings/profile" class={ "sidebar-link", templ.KV("active", activeNav == "profile" || activeNav == "settings") }>
							@components.IconUser("icon-sm")
							<span>الملف الشخصي والإعدادات</span>
						</a>
					</div>

				</nav>
			</aside>

			<!-- Main Content -->
			<main class="main-content">
				<header class="top-navbar">
					<div class="top-navbar-start">
						<h1 class="top-navbar-title">{ title }</h1>
					</div>

					<div class="top-navbar-end">
						@components.LanguageToggle(lang)
						@components.ThemeToggle()
						<span class="badge badge-emerald font-semibold">
							@components.IconCheckCircle("icon-xs")
							<span>مورد معتمد</span>
						</span>
						<form action="/auth/logout" method="POST" class="m-0 inline">
							<button type="submit" class="btn btn-secondary btn-sm btn-logout" title="تسجيل الخروج">
								@components.IconLogout("icon-xs")
								<span>تسجيل الخروج</span>
							</button>
						</form>
					</div>
				</header>

				<div class="page-container">
					{ children... }
				</div>
			</main>
		</div>
	}
	}
}
