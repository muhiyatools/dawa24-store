package layouts

import (
	"github.com/muhiya/dawa24-store/internal/ui/components"
)

// PharmacyShell is the unified portal shell for pharmacies and medical facilities.
templ PharmacyShell(title string, activeNav string, lang string, dir string, perms []string) {
	@Base(title, lang, dir) {
		<div class="app-shell">
			<!-- Sidebar -->
			<aside class="sidebar">
				<div class="sidebar-header" style="padding:1.25rem 1.25rem; display:flex; align-items:center; justify-content:space-between; border-bottom:1px solid var(--neutral-800);">
					<div style="display:flex; align-items:center; gap:0.75rem; overflow:hidden;">
						<img src="/static/img/logo.png" alt="دواء 24" style="height:46px; max-height:48px; object-fit:contain;" />
						<div style="display:flex; flex-direction:column;">
							<span style="font-weight:900; font-size:1.15rem; color:#ffffff; line-height:1.2; font-family:'Cairo', sans-serif;">دواء <span style="color:var(--primary-400);">24</span></span>
							<span style="font-size:0.75rem; color:var(--neutral-400); font-weight:700;">بوابة الصيدلية المعتمدة</span>
						</div>
					</div>
					<button type="button" class="btn btn-icon" data-sidebar-toggle style="background:rgba(255,255,255,0.08); color:#ffffff; width:34px; height:34px; border:none; border-radius:var(--radius-md); flex-shrink:0;" title="طي/توسيع القائمة">
						@components.IconMenu("icon-xs")
					</button>
				</div>

				<nav class="sidebar-nav" style="padding:1rem 0.75rem; display:flex; flex-direction:column; gap:0.25rem;">
					<a href="/pharmacy/dashboard" class={ "sidebar-link", templ.KV("active", activeNav == "dashboard") }>
						@components.IconHome("icon-sm")
						<span>لوحة التحكم الرئيسية</span>
					</a>
					<a href="/orders" class={ "sidebar-link", templ.KV("active", activeNav == "orders") }>
						@components.IconPackage("icon-sm")
						<span>طلبياتي وأوامر التوريد</span>
					</a>
					<a href="/suppliers/followed" class={ "sidebar-link", templ.KV("active", activeNav == "followed") }>
						@components.IconBuilding("icon-sm")
						<span>الموردون المتابعون</span>
					</a>
					<a href="/wallet" class={ "sidebar-link", templ.KV("active", activeNav == "wallet") }>
						@components.IconWallet("icon-sm")
						<span>المحفظة والمعاملات</span>
					</a>
					<a href="/favorites" class={ "sidebar-link", templ.KV("active", activeNav == "favorites") }>
						@components.IconHeart("icon-sm")
						<span>الأدوية المفضلة</span>
					</a>
					<a href="/invoices" class={ "sidebar-link", templ.KV("active", activeNav == "invoices") }>
						@components.IconFile("icon-sm")
						<span>الفواتير الإلكترونية</span>
					</a>
					<a href="/settings" class={ "sidebar-link", templ.KV("active", activeNav == "settings") }>
						@components.IconSettings("icon-sm")
						<span>الملف الشخصي والإعدادات</span>
					</a>
				</nav>

				<div style="border-top:1px solid var(--neutral-800); padding:1rem 0.75rem; display:flex; flex-direction:column; gap:0.5rem; margin-top:auto;">
					<a href="/catalog" class="btn btn-primary" style="width:100%; justify-content:center; font-weight:800; padding:0.65rem;">
						@components.IconSearch("icon-xs")
						<span>الكتالوج الدوائي الموحد</span>
					</a>
				</div>
			</aside>

			<!-- Main Workspace -->
			<main class="main-content">
				<header class="top-navbar" style="padding:1rem 2rem; display:flex; justify-content:space-between; align-items:center; background:#ffffff; border-bottom:1px solid var(--neutral-200); position:sticky; top:0; z-index:100;">
					<div>
						<h1 style="font-size:1.35rem; font-weight:900; color:var(--neutral-900); margin:0;">{ title }</h1>
					</div>

					<div style="display:flex; align-items:center; gap:0.75rem;">
						<div style="width:190px;">
							@components.ThemeToggle()
						</div>
						<a href="/cart" class="btn btn-secondary btn-icon" title="سلة المشتريات">
							@components.IconCart("icon-sm")
						</a>

						<a href="/wallet" class="btn btn-secondary" style="font-weight:800; background:var(--primary-50); color:var(--primary-800); border-color:var(--primary-200);">
							@components.IconWallet("icon-xs")
							<span class="tabular-nums">5,420.00 ج.م</span>
						</a>

						<form action="/auth/logout" method="POST" style="margin:0; display:inline;">
							<button type="submit" class="btn btn-secondary btn-sm" style="color:var(--danger); border-color:var(--danger-border); font-weight:800;">
								@components.IconLogout("icon-xs")
								<span>خروج</span>
							</button>
						</form>
					</div>
				</header>

				<div class="page-container" style="padding:2rem;">
					{ children... }
				</div>
			</main>
		</div>
	}
}
